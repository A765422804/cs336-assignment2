model:
  vocab_size: 10000
  context_length: 512
  d_model: 2560
  num_layers: 32
  num_heads: 32
  d_ff: 10240
  rope_theta: 10000

optimizer:
  lr: 0.001 
  betas: [0.9, 0.95]
  eps: 0.00000001
  weight_decay: 0.01

data:
  batch_size: 4

runtime:
  device: cuda

benchmark:
  warmup_steps: 5
  measured_steps: 10
  mode: forward # forward forward_backward full_step